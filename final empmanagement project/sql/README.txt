Install MYSQL and then create a database named as:

employeedb and run below command


mysql -u username -p database_name < employeedb.sql

Make sure, employedb.sql is present in the same path where you are executing this command. or provide some custom location in case it is not.

like below
c:/users/xyz/employeedb.sql